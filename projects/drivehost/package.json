{
  "name": "DriveHost",
  "version": "1.0.0",
  "description": "In order to run this project locally, you will have to setup your local eveironment following these steps. Make sure you have nodejs installed and type these commands in your terminal at the project root folder: (if you have gulp installed locally, you can ignore it) ```sh $ npm install gulp --save-dev  ``` gulp packages you would need are:",
  "main": "gulpfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-inject-partials": "^1.0.3",
    "gulp-less": "^3.4.0",
    "gulp-minify": "^2.1.0",
    "gulp-minify-css": "^1.2.4",
    "gulp-rename": "^1.2.2",
    "gulp-watch": "^4.3.11"
  }
}
